apt install sudo net-tools gnupg default-jre default-jdk mc -y
sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | apt-key add -
apt-get update
apt -y install postgresql

///	Изменить в /etc/postgresql/15/main/pg_hba.conf в строках с "127.0.0.1/32" и "::1/128" последнее значение на "trust"
service postgresql restart

psql -h localhost -U postgres -c "ALTER USER postgres PASSWORD 'D\!24013555';"
psql -h localhost -U postgres -c 'create database shiftgen_db'

adduser jvmapps   ///	Запомнить пароль
adduser jvmapps sudo
su - jvmapps	///	Вход в jvmapps
mkdir ./shiftgen

///	Залить файл jar на сервер пример: scp shiftgen-backend-all.jar jvmapps@5.8.10.211:/home/jvmapps/shiftgen
///	Если ругается на ssh-key, то удаляем его на компе: ssh-keygen -R 5.8.10.211
///	и повторяем команду заливки
///	Прямой запуск на сервере: java -jar ./shiftgen/shiftgen-backend-all.jar

///	Создание сервиса, содержание в файле shiftgen.service
///	Залить файл shiftgen.service на сервер пример: scp shiftgen.service root@5.8.10.211:/etc/systemd/system
sudo systemctl daemon-reload
sudo systemctl start shiftgen.service	/// stop - остановить, status - информация, enable - вкл. автозапуск, disable - выкл. автозапуск

